# config.toml

# 历史MCP服务的监听配置
[service]
host = "0.0.0.0"
port = 9100

# 模型配置
[models]
embedding = "./models/bge-small-zh-v1.5"
reranker = "./models/bge-reranker-base"

# 向量数据库 (Qdrant) 配置
[vector_db]
path = "./db/histories"
collection_name = "chat_histories"
